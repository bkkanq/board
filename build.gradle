// 공통 설정
// 최상위 build.gradle은 모든 프로젝트에서 사용할만한 공통 설정이나 Task 등이 들어가며, 각 프로젝트 내부의 build.gradle엔 각 프로젝트의 설정이 들어간다.
subprojects {
    // Topcat plugin 사용을 위한 설정?
    buildscript {
//        ext {
//            springBootVersion = '2.5.0-SNAPSHOT'
//        }
        repositories {
            // NOTE: You should declare only repositories that you need here
            //maven { url 'http://repo.linecorp.com/content/repositories/plugins.gradle.org/' }
            //maven { url 'http://repo.linecorp.com/content/repositories/releases/' }

            mavenLocal()
        }
//        dependencies {
//            classpath("org.springframework.boot:spring-boot-gradle-plugin:${springBootVersion}")
//            classpath "io.spring.gradle:dependency-management-plugin:1.0.6.RELEASE"
//        }
    }
    
    project.group = 'com.example'
    project.version = '0.0.1-SNAPSHOT'

    repositories {
        //가장 기본 Maven 리포지토리
        mavenCentral()

        //기타 Maven 리포지토리 URL 지정
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
}
